# Site settings
title: Vienna PHP User Group
description: ""
url: "http://viennaphp.org"

# Build settings
exclude: ['Rakefile', 'Vagrantfile']
timezone: Europe/Vienna
encoding: utf-8
markdown: kramdown

# Serve settings
host: 0.0.0.0
port: 4000
